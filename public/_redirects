# Cloudflare Pages redirects
# https://developers.cloudflare.com/pages/platform/redirects/

# www → apex
https://www.questron.app/* https://questron.app/:splat 301

# NOTE: Clean URLs (/host → host.html etc.) are handled automatically
# by Cloudflare Pages — no manual redirects needed.
